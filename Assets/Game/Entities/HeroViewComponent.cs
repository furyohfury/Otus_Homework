namespace Entities
{
    public sealed class HeroViewComponent
    {
        public HeroView HeroView;

        public HeroViewComponent(HeroView view)
        {
            HeroView = view;
        }
    }
}